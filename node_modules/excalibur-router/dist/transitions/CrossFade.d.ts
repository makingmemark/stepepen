import type { Engine } from 'excalibur';
import { ScreenElement } from 'excalibur';
import type { TransitionArgs } from './Transition.js';
import { Transition } from './Transition.js';
export interface CrossFadeArgs extends TransitionArgs {
    duration?: number;
}
export declare class CrossFadeTransition extends Transition {
    el: ScreenElement;
    screenshot: HTMLImageElement;
    constructor({ duration, ...args }?: CrossFadeArgs);
    onInitialize(engine: Engine): void;
    onIntroStart(): void;
    onOutroStart(): void;
    onIntro(progress: number): void;
    onOutro(progress: number): void;
    onOutroComplete(): void;
    private takeScreenshot;
}
